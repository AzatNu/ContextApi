import { createContext } from "react";
export const errorMessageModalWinowContext = createContext(null);
export const searchSortContext = createContext(null);
export const updateModalWindowContext = createContext(null);
